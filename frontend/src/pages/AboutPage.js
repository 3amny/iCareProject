import styled from "styled-components";

const AboutPage = () => {
  return (
    <Wrapper>
      <h4> About page</h4>
    </Wrapper>
  );
};

const Wrapper = styled.main``;
export default AboutPage;
